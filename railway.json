{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && tsc -p server/tsconfig.server.json && npm run build"
  },
  "deploy": {
    "startCommand": "node server/dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}